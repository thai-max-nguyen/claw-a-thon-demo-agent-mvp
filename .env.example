# LLM provider — GreenNode MaaS (OpenAI-compatible)
# Get LLM_API_KEY from the GreenNode AI Portal (MaaS API Key) or via /agentbase-llm.
# Leave LLM_API_KEY empty to run in stub mode (no live model calls).
LLM_BASE_URL=https://maas-llm-aiplatform-hcm.api.vngcloud.vn/v1
LLM_API_KEY=
LLM_MODEL=Qwen2.5-7B-Instruct
